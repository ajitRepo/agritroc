{
  "name": "web-agri-troc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/prepare-db.js && next dev",
    "build": "node scripts/prepare-db.js && prisma db push && prisma generate && next build",
    "start": "next start",
    "lint": "eslint",
    "db:generate": "node scripts/prepare-db.js && prisma generate",
    "db:push": "node scripts/prepare-db.js && prisma db push",
    "db:seed": "node scripts/prepare-db.js && tsx prisma/seed.ts",
    "db:studio": "node scripts/prepare-db.js && prisma studio",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "jose": "^5.9.0",
    "lucide-react": "^0.460.0",
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.4",
    "prisma": "^5.22.0",
    "tailwindcss": "^4",
    "tsx": "^4.23.1",
    "typescript": "^5"
  }
}
